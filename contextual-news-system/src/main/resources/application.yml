server:
  port: 8765

spring:
  application:
    name: contextual-news-system

  datasource:
    url: jdbc:postgresql://localhost:5434/news
    username: postgres
    password: 9890
#    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
flyway:
  baseline-on-migrate: true